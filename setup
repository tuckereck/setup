sudo apt update && sudo apt upgrade -y && sudo apt install dkms libelf-dev 
linux-headers-$(uname -r) build-essential gparted nwipe lvm2 madm 
btrfs-progs dcfldd -y && cd /media/tuckereck/'Parallels Tools' && sudo 
./install
